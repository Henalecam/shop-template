# Copy to .env and set your connection string from Railway
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public"
# Optional: primary domain used to detect subdomains (e.g., sistema.com)
PRIMARY_DOMAIN=""
# Server port
PORT=4000